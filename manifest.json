{
  "schemaVersion": 1,
  "id": "io.github.ldzhouquan.fast-browser",
  "name": "Fast Browser",
  "version": "0.2.0",
  "description": "Jev-powered browser agent for PI-Desktop: dynamic indexed action space, one TypeSafe request per decision, work-panel browser via CDP. Tools: jev_observe, jev_act, jev_run, jev_wait, jev_cancel.",
  "i18n": {
    "en": {
      "name": "Fast Browser",
      "description": "Jev-powered browser agent for PI-Desktop: dynamic indexed action space, one TypeSafe request per decision, work-panel browser via CDP. Tools: jev_observe, jev_act, jev_run, jev_wait, jev_cancel.",
      "safetyNotes": "Drives PI-Desktop's built-in work-panel browser through the host-approved CDP surface and sends page snapshots to api.typesafe.ai (Jev routing) over net.fetch. Field text may be generated by the host-configured model via agent.complete. No filesystem access; network is limited to api.typesafe.ai; one run at a time in the shared, visible browser."
    },
    "zh-CN": {
      "name": "Fast Browser",
      "description": "基于 Jev 的 PI-Desktop 浏览器智能体：动态编号动作空间，每次决策一次 TypeSafe 请求，通过 CDP 驱动工作面板浏览器。工具：jev_observe、jev_act、jev_run、jev_wait、jev_cancel。",
      "safetyNotes": "通过宿主许可的 CDP 接口驱动内置工作面板浏览器，并将页面快照发送至 api.typesafe.ai（Jev 路由，net.fetch 域名白名单仅此一个）。填写文本时可能调用宿主已配置模型（agent.complete）。不访问文件系统；同一时间只运行一个任务，浏览器全程可见。"
    }
  },
  "categories": [
    "developer-tools",
    "community"
  ],
  "changelog": "0.2.0: full Jev loop (jev_observe / jev_act / jev_run / jev_wait / jev_cancel), page-freshness guards, host-model text fill, run progress events on the PI-Desktop bus.",
  "author": "ldzhouquan",
  "license": "MIT",
  "main": "main.js",
  "permissions": [
    "agent.tool.register",
    "browser.cdp",
    "net.fetch",
    "agent.complete",
    "models.list",
    "bus.publish",
    "background.service"
  ],
  "net": {
    "domains": [
      "api.typesafe.ai"
    ]
  },
  "engines": {
    "piDesktop": ">=0.2.0"
  },
  "contributes": {
    "agentTools": [
      {
        "name": "jev_observe",
        "description": "Observe the work-panel browser page and return the Jev-style numbered element table (index, role, label, value, operations) plus page text preview. Call after navigate."
      },
      {
        "name": "jev_act",
        "description": "Execute one observed action on the work-panel browser: CLICK / TYPE_TEXT / SELECT / SCROLL / WAIT. Pass an element index from jev_observe (e.g. \"3\", \"6:2\") or a control id (\"scroll_down\", \"wait\"), with text for TYPE_TEXT. Re-checks page freshness before acting."
      },
      {
        "name": "jev_run",
        "description": "Run the full Jev browser-agent loop on the work-panel browser: navigate to url, then observe -> Jev decision (TypeSafe API) -> execute -> repeat until DONE/BLOCKED/step budget. Returns immediately with a runId; poll with jev_wait."
      },
      {
        "name": "jev_wait",
        "description": "Wait for a jev_run run to finish or time out. Returns current status, step history, and the latest element table."
      },
      {
        "name": "jev_cancel",
        "description": "Cancel a running jev_run loop and release the work-panel browser."
      }
    ],
    "bus": {
      "publish": [
        "fast-browser.run.progress",
        "fast-browser.run.done",
        "fast-browser.run.error"
      ]
    },
    "settings": [
      {
        "key": "typesafeKey",
        "title": "TypeSafe API Key",
        "type": "string",
        "default": "",
        "description": "TypeSafe API key for Jev decision routing (https://api.typesafe.ai). Required for jev_run."
      },
      {
        "key": "typesafeModel",
        "title": "TypeSafe Model",
        "type": "string",
        "default": "jev-latest",
        "description": "TypeSafe model id used for routing decisions."
      },
      {
        "key": "maxSteps",
        "title": "Max Steps",
        "type": "number",
        "default": 60,
        "description": "Hard step budget per run (jev-ultrafast default 60)."
      }
    ],
    "services": [
      {
        "id": "runner"
      }
    ]
  }
}
